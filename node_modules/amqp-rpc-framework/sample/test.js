const MessageRouter = require('../router.js')

const mq = new MessageRouter()

mq.setHandler("createUser", async (args) => {
    console.log('executing function');
    console.log(args.toString());
    return { message: "hello" }
});

mq.listen("amqp://127.0.0.1:5672", "user");
console.log('listening to queue <user>')